/*
 * CardComp Messages
 *
 * This contains all the text for the CardComp component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardComp';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardComp component!',
  },
});
