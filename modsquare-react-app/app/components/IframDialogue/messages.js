/*
 * IframDialogue Messages
 *
 * This contains all the text for the IframDialogue component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.IframDialogue';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IframDialogue component!',
  },
});
