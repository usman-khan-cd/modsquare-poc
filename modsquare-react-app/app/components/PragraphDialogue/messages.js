/*
 * PragraphDialogue Messages
 *
 * This contains all the text for the PragraphDialogue component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PragraphDialogue';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PragraphDialogue component!',
  },
});
