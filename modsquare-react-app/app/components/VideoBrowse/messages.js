/*
 * VideoBrowse Messages
 *
 * This contains all the text for the VideoBrowse component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.VideoBrowse';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VideoBrowse component!',
  },
});
