/*
 * ContentUploader Messages
 *
 * This contains all the text for the ContentUploader container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ContentUploader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContentUploader container!',
  },
});
