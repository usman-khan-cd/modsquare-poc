/*
 * Moderator Messages
 *
 * This contains all the text for the Moderator container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Moderator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Moderator container!',
  },
});
